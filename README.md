# Docker Image - Jupyter Notebook + Tensorflow + Scala + Kotlin
## Usage
    $ export JUPYTER_PASS=password
    $ export LOCAL_DATA_PATH=/path/to/datafolder
    $ docker-compose up -d
    $ open  http://localhost:8888/
